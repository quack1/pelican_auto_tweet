# Add issues

- Create URL with Pelican method

# Issue

- Best handling of error cases
- Pelican_auto_tweet : Use a git library
- Pelican_auto_tweet : Meilleure gestion des drafts
- Plusieurs scripts : 'if conf.blog_directory' gerer le cas ou conf n'existe pas ou n'est pas importé
- Libtwitter

# v1.0

- Rename the project : 'pelican_auto_tweet' -> 'pelican-twitter' or 'pelican-triggers' or 'pelican-social'
- Add custom git/publish command on the conf file
- Add a special class for images in blogpost (example : twitter-pic) and add these images to the tweets sent by pelican_auto_tweet and pelican_tweet_summary below the link and title.

